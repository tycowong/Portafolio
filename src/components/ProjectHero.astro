---
import type { ImageMetadata } from "astro";
import { Image } from "astro:assets";
interface Props {
  img: string;
  alt: string;
}

const { img, alt } = Astro.props;
---

<img
  src=`/images/${img}`
  alt=`${alt}`
  class="rounded-[30px] p-5"
  transition:name=`img-${img}`
/>
